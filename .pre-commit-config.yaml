# 1. pip install pre-commit
# 2. pre-commit install
# 3. set GITGUARDIAN_API_KEY in your environment
repos:
  - repo: https://github.com/gitguardian/ggshield
    rev: v1.11.0
    hooks:
      - id: ggshield
        language_version: python3
        stages: [commit]
